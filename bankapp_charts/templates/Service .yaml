apiVersion: v1
kind: Service
metadata:
  name: {{values.name}}
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: {{{values.name}}
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8080
      nodeport: 30101