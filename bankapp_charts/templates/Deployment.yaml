apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{values.name}}
  labels:
    app: {{values.name}}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: {{values.name}}
  template:
    metadata:
      labels:
        app: {{values.name}}
    spec:
      containers:
      - name: javaapp-server
        image: {{values.deployment.containers.imagename}}
        ports:
        - containerPort: 8080
